{
  "name": "@codemcp/workflows-core",
  "version": "6.6.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "files": [
    "dist/**/*",
    "resources/**/*"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json && pnpm copy-workflows && pnpm copy-agents",
    "copy-workflows": "rm -rf resources/workflows resources/templates && mkdir -p resources && cp -r ../../resources/workflows ../../resources/templates resources/",
    "copy-agents": "rm -rf resources/agents && mkdir -p resources && cp -r ../../resources/agents resources/",
    "dev": "tsc --watch",
    "clean:build": "rimraf ./dist ./resources",
    "test": "vitest --run",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "format:check": "prettier --check .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@types/js-yaml": "4.0.9",
    "js-yaml": "4.1.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "vitest": "4.0.18"
  },
  "private": true
}
